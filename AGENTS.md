# Repository workflow

- After modifying project files, validate the relevant behavior, commit only the intended changes, and push the commit to `origin`.
- Use concise commit messages that describe the user-visible outcome.
- Never commit credentials, local secrets, unrelated files, or temporary validation artifacts.
